# ai-chat プロジェクト仕様書

## 概要

ユーザーがオリジナルキャラクターを作成し、会話を楽しめるAIチャットボットサービス。

### 基本情報

| 項目 | 内容 |
|------|------|
| サービス種別 | エンターテインメント（キャラクター会話） |
| 対象ユーザー | 一般公開 |
| 認証 | 不要（匿名利用） |
| 料金 | 完全無料 |

## 技術スタック

### フレームワーク・ライブラリ

| 用途 | 技術 |
|------|------|
| フロントエンド | Next.js (App Router) |
| バックエンド | Next.js (App Router) + Hono |
| ORM | Prisma.js |
| AIエージェント | Mastra |
| AIモデル | Google Gemini |

### インフラ

| 項目 | 内容 |
|------|------|
| ホスティング | Google Cloud Run |
| コンテナ | Docker |

## 機能仕様

### 画面構成

2ステップ構成（トップページなし）:

1. **キャラクター作成画面** - キャラクターの設定を行う
2. **会話画面** - 作成したキャラクターとチャット

### キャラクター作成

#### 設定項目

| 項目 | 必須 | 説明 |
|------|------|------|
| 名前 | ○ | キャラクターの名前 |
| 性格・口調 | ○ | キャラクターの性格と話し方 |

#### テンプレートキャラクター

ユーザーがすぐに始められるよう、以下のテンプレートを用意:

1. **ツンデレ幼馴染** - 素直になれないけど実は優しい。照れ隠しで冷たく言うことも
2. **クールな執事** - 丁寧で冷静な対応。たまに毒舌を交える
3. **元気な妖精** - 明るくポジティブ。ちょっとおっちょこちょい
4. **ミステリアスな占い師** - 意味深で神秘的な言葉遣い
5. **熱血スポーツコーチ** - 情熱的で常に励ましてくれる

### 会話機能

#### レスポンス

- **表示方式**: ストリーミング（文字が流れるように表示）
- **最大トークン**: 500トークン/回

#### 制限

| 項目 | 制限値 |
|------|--------|
| 1回の返答 | 最大500トークン |
| 会話往復数 | 最大20往復/セッション |

### データ保持

| データ | 保持期間 |
|--------|----------|
| キャラクター設定 | セッション中のみ |
| 会話履歴 | セッション中のみ |

※ページ離脱・リロードでデータは消去される

## UI/UXガイドライン

### デザインテーマ

- **スタイル**: ポップ・カラフル
- 明るく楽しい雰囲気
- 親しみやすい配色

### 画面遷移

```
[キャラクター作成画面]
    ↓ 作成完了
[会話画面]
```

## ディレクトリ構成（推奨）

```
ai-chat/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── page.tsx            # キャラクター作成画面
│   │   ├── chat/
│   │   │   └── page.tsx        # 会話画面
│   │   ├── api/
│   │   │   └── [...route]/
│   │   │       └── route.ts    # Hono API routes
│   │   ├── layout.tsx
│   │   └── globals.css
│   ├── components/             # UIコンポーネント
│   │   ├── character/          # キャラクター関連
│   │   └── chat/               # チャット関連
│   ├── lib/
│   │   ├── mastra/             # Mastra設定・エージェント
│   │   ├── prisma/             # Prismaクライアント
│   │   └── hono/               # Honoアプリ設定
│   ├── types/                  # 型定義
│   └── constants/              # 定数（テンプレートなど）
├── prisma/
│   └── schema.prisma
├── public/
├── package.json
├── tsconfig.json
├── next.config.js
└── CLAUDE.md
```

## 開発ガイドライン

### コーディング規約

- TypeScriptを使用
- 関数コンポーネントとHooksを使用
- 命名規則: キャメルケース（変数・関数）、パスカルケース（コンポーネント）

### 環境変数

```env
# Gemini API
GOOGLE_GENERATIVE_AI_API_KEY=your_api_key

# Database (必要に応じて)
DATABASE_URL=your_database_url
```

### セキュリティ

- `.claudeignore`で機密ファイルをClaude Codeの読み取り対象から除外
- 対象ファイル: `.env`, `.env.local`, `.env.*.local`, `*.pem`, `*.key`
- APIキーなどの機密情報は`.env.local`に記載し、Gitにコミットしない

### Git運用

- コミットメッセージは日本語で簡潔に
- main/masterへの直接プッシュは避ける
- **タスク完了時のルール**: 各フェーズまたはタスク完了後、動作確認を行った上でコミット・プッシュする

## デプロイ

### Google Cloud Run設定

- Dockerコンテナでデプロイ
- 環境変数はCloud Runのシークレットまたは環境変数で設定
- 自動スケーリング対応

### ビルド・デプロイ

```bash
# ローカルビルド確認
npm run build

# Cloud Runへデプロイ（gcloud CLI使用）
gcloud run deploy ai-chat \
  --source . \
  --region asia-northeast1 \
  --allow-unauthenticated \
  --set-env-vars "GOOGLE_GENERATIVE_AI_API_KEY=your_api_key"
```

### URL

https://ai-chat-654264548176.asia-northeast1.run.app/
